<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicle.dicle.hafiza_oyunu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dogru"
        android:label="@string/app_name"
         android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MatematikselIslem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Gorsel_Islem" />
        <activity android:name=".GorselHafiza" />
        <activity android:name=".MatematikselHafizaSecimSayfasi" />
        <activity android:name=".SelectPage" />
        <activity android:name=".SonucDegerlendirme"/>

    </application>

</manifest>